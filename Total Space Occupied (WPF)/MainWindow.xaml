<Window x:Class="TotalSpaceOccupied.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TotalSpaceOccupied"
        mc:Ignorable="d"
        Title="Total Space Occupied" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="PathsTextBox"
                 Grid.Row="0"
                 AcceptsReturn="True"/>
        <StackPanel Orientation="Horizontal"
                    Grid.Row="1">
            <TextBlock Width="120">Total Space Occupied:</TextBlock>
            <TextBlock x:Name="TotalSpaceOccupied"
                       Text="0 bytes"/>
        </StackPanel>
    </Grid>
</Window>